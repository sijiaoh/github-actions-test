name: Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::add-mask::${{secrets.SECRETS}}"

      - name: Set STAGE environment
        run: echo "STAGE=${GITHUB_REF#refs/heads/}" >> $GITHUB_ENV
      - run: echo ${STAGE}
      - run: echo ${{env.STAGE}}
      - name: Set SECRETS
        run: echo "${{secrets.SECRETS}}" >> $GITHUB_ENV
      - run: echo ${{env.A}}
      - run: echo ${{env.B}}
